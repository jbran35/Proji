@using BlazorBootstrap
@inherits AppComponentBase

<Modal @ref="_newAssigneeModal" Title="New Assignee">
    <BodyTemplate>
        <p>Email:</p>
        <div>
            <div class="d-flex mb-3 gap-2">
                <input type="text"
                       class="form-control"
                       placeholder="johndoe@gmail.com"
                       @bind="_email"
                       disabled="@_assigneeFound" />

                <Button Type="ButtonType.Submit" Color="ButtonColor.Dark" @onclick="SearchForUser" Disabled="@_assigneeFound" aria-label="SearchForUser"><Icon Name="IconName.Search" /></Button>

            </div>
        </div>
    </BodyTemplate>
    <FooterTemplate>
        <Button Color="ButtonColor.Secondary" @onclick="OnCancelClick" aria-label="CloseNewAssigneeModal">Close</Button>
        <Button Color="ButtonColor.Dark" @onclick="OnSaveAssigneeModalClick" aria-label="AddNewAssignee" Disabled="@(!_assigneeFound)">Add</Button>
    </FooterTemplate>
</Modal>